somaAltura = 0
	peso70 = 0

for i = 1:20
    altura(i) = input('informe uma altura em cm:')
    peso(i) = input('informe um peso em kg:')
    somaAltura = idade(i)+soma
    if(peso(i)==70)
 	        peso70 = peso70 + 1
    endif
endfor

maiorAltura = max(altura)
menorAltura = min(altura)
media = somaAltura / length(altura)
	
